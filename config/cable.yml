development:
  adapter: redis
  url: redis://localhost:6379/1 # redis://localhost|redis:6379/1 in Docker

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %> # redis://localhost|redis:6379/1 in local|Docker
  channel_prefix: fakebook_production
